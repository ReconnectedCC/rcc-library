package cc.reconnected.library;

import cc.reconnected.library.config.Config;

@Config(RccLib.MOD_ID)
public class RccLibConfig {
    public String link = "<c:#8888ff><u>${label}</u></c>";
    public String linkHover = "${url}";
}
